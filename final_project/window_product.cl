__kernel void
window_product(__global uchar * input, __global int cols, __global int rows){
	int id = get_global_id(0);
	if( id % rows == 0)
	if( id
	int W_11 =
	int x = id % 3;
	int y = id / 3;

	
}
